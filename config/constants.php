<?php
// Define the base path of the project
define("BASE_PATH", realpath(__DIR__ . "/.."));

// Define the path to the Views directory
define("VIEW_PATH", BASE_PATH . "/app/Views");

// Define the path to the public directory
define("PUBLIC_PATH", BASE_PATH . "/public");

define("STORAGE_PATH", BASE_PATH . "/storage");

// Define the base URL for the public folder
define("BASE_URL", "/ghmp/public");

define("VIEW_URL", BASE_URL . "/../app/Views");


// Define the base URL for the public folder
define("VENDOR_URL", "/ghmp/vendor");

